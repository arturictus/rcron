#!/usr/bin/env ruby

require "bundler/setup"
require "rcron"
require 'erlectricity'

receive do |f|
  f.when([:execute, String]) do |command|
    f.send!([:result, [:ok, eval(command)]])
    f.receive_loop
  end
end
